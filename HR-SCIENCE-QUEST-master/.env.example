# Copy to `.env` for local dev, or set in your hosting provider for prod.
# All client-side env vars in CRA must be prefixed REACT_APP_.

# Backend API base URL (no trailing slash)
REACT_APP_API_BASE_URL=http://127.0.0.1:8000
